THESE ARE ECLIPSE PROJECTS, just make your workspace a1, and p1 and p2 will appear

To run:
/usr/lib/jvm/java-8-oracle/bin/java -Xmx4G -Dfile.encoding=ISO-8859-1 -classpath ABSOLUTE_PATH/a1.modified/p2/bin:ABSOLUTE_PATH/a1.modified/commons-cli-1.3.1.jar main -NUMBINS X_MANY_BINS -DATASIZE Y_MANY_ELEMENTS -NUMTHREADS Z_MANY_THREADS

The ant files are generated from eclipse, however you run into problems with heap size for 700M elements, the above command will run it.  If you are using the ant file:
Args are toward the bottom of the ant file
